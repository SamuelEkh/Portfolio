export type IProject = {
  title: string,
  date: string,
  img: string,
  description: string,
  tech: string,
  codelink: string,
  deploylink?: string
}

export type IContact = {
  name: string,
  email: string,
  message: string
}